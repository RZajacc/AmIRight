import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Issues",
  description: "Current issues",
};

function Issues() {
  return <main>Issues</main>;
}

export default Issues;
